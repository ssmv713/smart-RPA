import { CustomButton } from "../../../common/components/buttons/CustomButton";

export const MembersView = () => {
  return (
    <div>
      <p>{"Members View Content"}</p>
      <CustomButton variant={"contained"}>{"Button Text"}</CustomButton>
    </div>
  );
};
